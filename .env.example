# Hermes Control Plane 0.5.11-dev.5
# Copy to .env, then run ./hermesctl init. Never commit .env.

# Project-owned images. Set this to your Docker Hub username/organization after publishing.
IMAGE_NAMESPACE=afsharidevops
VERSION=0.5.11-dev.5

# Runtime router selection: nine-router | omniroute
HERMES_ROUTER_PROVIDER=nine-router
HERMES_ENABLE_BOTH_ROUTERS=false

# Project endpoints (loopback by default)
CONTROL_PLANE_BIND_IP=127.0.0.1
CONTROL_PLANE_PORT=8800
ROUTER_GATEWAY_BIND_IP=127.0.0.1
ROUTER_GATEWAY_PORT=8790
SMART_ROUTER_BIND_IP=127.0.0.1
SMART_ROUTER_PORT=8787
HERMES_BIND_IP=127.0.0.1
HERMES_API_PORT=8642
HERMES_DASHBOARD_PORT=9119
KUBERNETES_BROKER_BIND_IP=127.0.0.1
KUBERNETES_BROKER_PORT=8830

# Generated by ./hermesctl init
HERMES_CONTROL_ADMIN_TOKEN=CHANGE_ME
# Service identity used only by the Hermes Bot for Kubernetes/Helm planning/execution.
HERMES_BOT_SERVICE_TOKEN=CHANGE_ME
# Separate identity used by the Approval Bot. Do not expose this token to the main Hermes Bot.
HERMES_APPROVAL_BOT_TOKEN=CHANGE_ME
HERMES_APPROVAL_HMAC_KEY=CHANGE_ME
# Separate signing key for agent task envelopes.
HERMES_AGENT_TASK_HMAC_KEY=CHANGE_ME
# Credential Service has a distinct operator/admin identity and a narrow metadata-sync identity.
HERMES_CREDENTIAL_ADMIN_TOKEN=CHANGE_ME
HERMES_CREDENTIAL_SERVICE_TOKEN=CHANGE_ME
# hermesctl init generates a Fernet-compatible urlsafe-base64 32-byte master key.
HERMES_CREDENTIAL_MASTER_KEY=CHANGE_ME
HERMES_CREDENTIAL_MASTER_KEY_VERSION=v1
CREDENTIAL_SERVICE_BIND_IP=127.0.0.1
CREDENTIAL_SERVICE_PORT=8789
HERMES_KUBERNETES_BROKER_TOKEN=CHANGE_ME
HERMES_EXECUTION_HMAC_KEY=CHANGE_ME
HERMES_PROVIDER_WORKER_TOKEN=CHANGE_ME
HERMES_PROVIDER_EXECUTION_ENABLED=false
HERMES_PROVIDER_COMMAND_TIMEOUT=1800
HERMES_PROVIDER_SSH_PROFILE_HOST_PATH=./data/provider-ssh
HERMES_INFRASTRUCTURE_EXECUTION_ENABLED=false
HERMES_INFRASTRUCTURE_CREDENTIAL_HOST_PATH=./data/infrastructure-credentials
HERMES_INFRASTRUCTURE_ALLOW_HTTP=false
HERMES_INFRASTRUCTURE_REQUEST_TIMEOUT_SECONDS=20
HERMES_INFRASTRUCTURE_IPMI_TIMEOUT_SECONDS=20
HERMES_INFRASTRUCTURE_VERIFY_ATTEMPTS=5
HERMES_INFRASTRUCTURE_VERIFY_DELAY_SECONDS=1
HERMES_INFRASTRUCTURE_FIRMWARE_VERIFY_ATTEMPTS=60
HERMES_INFRASTRUCTURE_FIRMWARE_VERIFY_DELAY_SECONDS=5
HERMES_INFRASTRUCTURE_PLATFORM_VERIFY_ATTEMPTS=90
HERMES_INFRASTRUCTURE_PLATFORM_VERIFY_DELAY_SECONDS=5
HERMES_PROVIDER_FILES_REPO_URL=
HERMES_PROVIDER_APT_REPO_URL=
HERMES_PROVIDER_RPM_REPO_URL=
HERMES_PROVIDER_PYPI_URL=
# Comma-separated exact HTTPS hosts allowed for artifact downloads. Empty = network artifact fetch disabled.
HERMES_ARTIFACT_HTTPS_HOST_ALLOWLIST=
# Comma-separated exact OCI registry hosts allowed as mirror sources/destinations. Empty = OCI mirror disabled.
HERMES_ARTIFACT_OCI_SOURCE_REGISTRY_ALLOWLIST=
HERMES_ARTIFACT_OCI_DESTINATION_REGISTRY_ALLOWLIST=
# Trusted artifact credentials/keyrings are mounted read-only under this host path.
HERMES_ARTIFACT_AUTH_HOST_PATH=./data/artifact-auth
# Values below, when set, must be absolute container paths under /run/secrets/hermes-artifact-auth.
HERMES_ARTIFACT_HTTPS_AUTHFILE=
HERMES_ARTIFACT_REPOSITORY_KEYRING=
HERMES_ARTIFACT_OCI_SOURCE_AUTHFILE=
HERMES_ARTIFACT_OCI_DESTINATION_AUTHFILE=
HERMES_ARTIFACT_MIRROR_MAX_BYTES=536870912
HERMES_ARTIFACT_MIRROR_TIMEOUT_SECONDS=60
HERMES_ARTIFACT_REPOSITORY_MAX_EXPANDED_BYTES=4294967296
HERMES_ARTIFACT_REPOSITORY_METADATA_MAX_BYTES=268435456
ROUTER_GATEWAY_ADMIN_TOKEN=CHANGE_ME
SMART_ROUTER_HMAC_SECRET=CHANGE_ME
SMART_ROUTER_CLIENT_API_KEY=CHANGE_ME
SMART_ROUTER_ADMIN_API_KEY=CHANGE_ME
SMART_ROUTER_BOOTSTRAP_ADMIN_USER=admin
SMART_ROUTER_BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME

# Smart Router behavior. Neutral aliases are translated by router-gateway.
SMART_ROUTER_MODE=observe
SMART_ROUTER_POLICY=heuristic
SMART_ROUTER_OBSERVE_MODEL=hermes/observe
SMART_ROUTER_FAST_MODEL=hermes/fast
SMART_ROUTER_STANDARD_MODEL=hermes/standard
SMART_ROUTER_STRONG_MODEL=hermes/strong
SMART_ROUTER_CODING_MODEL=hermes/coding
SMART_ROUTER_VISION_MODEL=hermes/vision

# 9router upstream image and bootstrap configuration
NINEROUTER_IMAGE_REPOSITORY=decolua/9router
NINEROUTER_IMAGE_TAG=latest
NINEROUTER_BIND_IP=127.0.0.1
NINEROUTER_PORT=20128
NINEROUTER_INITIAL_PASSWORD=CHANGE_ME
NINEROUTER_JWT_SECRET=CHANGE_ME
NINEROUTER_API_KEY_SECRET=CHANGE_ME
NINEROUTER_MACHINE_ID_SALT=CHANGE_ME
NINEROUTER_REQUIRE_API_KEY=false
NINEROUTER_AUTH_COOKIE_SECURE=false
NINEROUTER_PUBLIC_BASE_URL=http://localhost:20128

# Automatically provision a dedicated 9router API key for Router Gateway.
NINEROUTER_AUTO_PROVISION_API_KEY=true
NINEROUTER_MANAGED_API_KEY_NAME=hermes-control-plane-router-gateway
NINEROUTER_PROVISION_HOST=127.0.0.1
# Seed/reconcile the Hermes routing objects expected by router-gateway.
# `ai` tracks the current OpenCode free pool; tier combos are created once and
# then preserved so dashboard/operator customization survives future starts.
NINEROUTER_AUTO_PROVISION_COMBOS=true
NINEROUTER_OPENCODE_CATALOG_URL=https://opencode.ai/zen/v1/models
# Managed by ./hermesctl. Do not fill this manually.
NINE_ROUTER_UPSTREAM_API_KEY=

# OmniRoute upstream image and bootstrap configuration
OMNIROUTE_IMAGE_REPOSITORY=diegosouzapw/omniroute
OMNIROUTE_IMAGE_TAG=latest
OMNIROUTE_BIND_IP=127.0.0.1
OMNIROUTE_PORT=20228
OMNIROUTE_API_BIND_IP=127.0.0.1
OMNIROUTE_API_PORT=20229
OMNIROUTE_INITIAL_PASSWORD=CHANGE_ME
OMNIROUTE_JWT_SECRET=CHANGE_ME
OMNIROUTE_API_KEY_SECRET=CHANGE_ME
# Internal OmniRoute MCP/A2A credential; not used as Router Gateway runtime key.
OMNIROUTE_MANAGEMENT_API_KEY=CHANGE_ME
OMNIROUTE_STORAGE_ENCRYPTION_KEY=CHANGE_ME
OMNIROUTE_STORAGE_ENCRYPTION_KEY_VERSION=v1
OMNIROUTE_MACHINE_ID_SALT=CHANGE_ME
OMNIROUTE_WS_BRIDGE_SECRET=CHANGE_ME
OMNIROUTE_REQUIRE_API_KEY=false
OMNIROUTE_AUTH_COOKIE_SECURE=false
OMNIROUTE_PUBLIC_BASE_URL=http://localhost:20228
OMNIROUTE_ALLOW_API_KEY_REVEAL=false
OMNIROUTE_MEMORY_MB=512

# Automatically provision a dedicated, non-management OmniRoute API key
# for Router Gateway through OmniRoute's authenticated management API.
OMNIROUTE_AUTO_PROVISION_API_KEY=true
OMNIROUTE_MANAGED_API_KEY_NAME=hermes-control-plane-router-gateway
OMNIROUTE_PROVISION_HOST=127.0.0.1
# Managed by ./hermesctl. Do not fill this manually.
OMNIROUTE_UPSTREAM_API_KEY=

# Hermes upstream agent (optional profile, enabled by hermesctl by default)
HERMES_IMAGE_REPOSITORY=nousresearch/hermes-agent
HERMES_IMAGE_TAG=latest
HERMES_DASHBOARD=0
HERMES_UID=10000
HERMES_GID=10000
# Hermes uses internal Smart Router automatic routing.
HERMES_MODEL=auto

# Hermes Telegram gateway. TELEGRAM_BOT_TOKEN is never generated automatically.
# Configure it locally with: ./hermesctl bot telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=

# Comma/newline-separated numeric Telegram user IDs allowed to ask Hermes Bot for infra mutation.
# ./hermesctl init / bot allow keeps these users included in TELEGRAM_ALLOWED_USERS.
HERMES_CONTROL_PLANE_BOT_USERS=

# Privileged execution is opt-in in beta development. Keep both false until you have reviewed the target and policy.
EXECUTION_FEATURES=
HERMES_EXECUTION_ENABLED=false
HERMES_KUBERNETES_EXECUTION_ENABLED=false
HERMES_KUBERNETES_COMMAND_TIMEOUT=60
# Filled by ./hermesctl init for Docker/Compose local kubeconfig access.
HERMES_KUBERNETES_BROKER_UID=
HERMES_KUBERNETES_BROKER_GID=

# Control Plane integration health probe timeout
HERMES_HEALTH_TIMEOUT_SECONDS=5

# Multi-cluster Kubernetes client matrix. The broker dynamically selects a
# compatible kubectl per target based on the live kube-apiserver minor version.
# Override these build args when you need different patch releases, then rebuild
# the kubernetes-broker image. Runtime selection is automatic per target.
KUBECTL_V1_33=v1.33.13
KUBECTL_V1_34=v1.34.10
KUBECTL_V1_35=v1.35.6
KUBECTL_V1_36=v1.36.2
KUBECTL_BOOTSTRAP_MINOR=1.34
HERMES_DYNAMIC_KUBECTL_ENABLED=true
HERMES_KUBECTL_SELECTION_MODE=exact-preferred
HERMES_KUBECTL_CACHE_TTL_SECONDS=10
